interface:
  display_name: "Dream"
  short_description: "Review work history for user friction"
  default_prompt: "Use $dream to review recent work for user friction and propose remedies."

policy:
  allow_implicit_invocation: false
